Legacy desktop GUI kept for compatibility and reference.

New live-control work should happen in the browser app at the project root. Use
`Start Legacy Desktop GUI.bat` only when you need to compare old behavior or run
an older desktop workflow.
